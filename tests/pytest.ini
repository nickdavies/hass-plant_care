[pytest]
# The model package imports no Home Assistant, so these run with nothing but
# pytest and voluptuous — no framework mocking, and the type hints in the code
# under test are the real ones.
testpaths = tests
addopts = -p no:homeassistant
